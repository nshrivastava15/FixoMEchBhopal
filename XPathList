import React from "react";
import { List, Card } from "antd";

export default function XPathList({ data }) {
  return (
    <Card>
      <List
        bordered
        dataSource={data}
        renderItem={(item) => <List.Item>{item}</List.Item>}
      />
    </Card>
  );
}
